# Base URL of the Study Companion backend (SvelteKit + Prisma).
# Must be an origin your deployment has registered with the backend's
# FRONTEND_ORIGIN env var, or every request will be silently blocked by CORS.
PUBLIC_API_BASE_URL=https://your-backend.example.com
